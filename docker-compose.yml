services:
  skycom-crewai:
    build: .
    container_name: skycom-crewai
    env_file: .env
    volumes:
      - .:/app
      - ./skycom:/app/skycom  # QUAN TRỌNG: Mount dự án Rails vào đây
    environment:
      - PYTHONUNBUFFERED=1